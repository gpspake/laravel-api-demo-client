import React from 'react';
import WidgetList from './Widgets/WidgetList'

const App = React.createClass({
  render() {
    return (
      <div className="App">
        <WidgetList />
      </div>
    );
  }
})

export default App;
